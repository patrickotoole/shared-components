directory author: victor

- intraweek_update.get_compiled_pacing_reports() = returns a PANDAS dataframe with pacing 
reports for all active (hard-coded) advertisers
- intraweek_update.get_advertiser_table(advertiser_id) = returns a PANDAS dataframe with
the past three weeks of advertiser activity, and this week's projected multiplier/charge
- cron_update_advertisers.py = python script to write multipler/cpa_target values into 
'intraweek' database

HANDLERS are found in: ~/streaming-python/metrics/handlers/admin/scripts
TEMPLATES found in: ~/streaming-python/metrics/templates
